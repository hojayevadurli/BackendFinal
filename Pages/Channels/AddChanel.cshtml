@page
@model Final.Pages.Channels.AddChanelModel

@{
}

@*<h1>@(Model.ChannelName ?? "[No channel is specified]")- @(Model.TopicName?? "[No topic is specified]"</h1>

    <ul>
        @foreach (var )
    </ul>*@


@*<h4>Add Parent</h4>
    <div class="col-md-6 col-sm-12 mx-auto mb-5">
        <form method="post" asp-page-handler="AddParent">
            <div class="mb-3 row">
                <div class="col-sm-4 text-sm-end align-self-center">
                    <label class="form-label" asp-for="Channel"></label>
                </div>
                <div class="col">
                    <input class="form-control" asp-for="Channel" />
                    <span class="form-text" asp-validation-for="Channel"></span>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-4 text-sm-end align-self-center">
                </div>
                <div class="col-8">
                    <button type="submit" class="btn btn-outline-primary">Add New Parent</button>
                </div>
            </div>
        </form>
    </div>

    @if (Model.Channel != null)
    {
        <h4>Add Child to @Model.Channel</h4>
        <div class="col-md-6 col-sm-12 mx-auto mb-5">
            <form method="post" asp-page-handler="AddChild">
                <input type="hidden" name="ParentName" value="@Model.Channel" />
                <div class="mb-3 row">
                    <div class="col-sm-4 text-sm-end align-self-center">
                        <label class="form-label" asp-for="Channel"></label>
                    </div>
                    <div class="col">
                        <input class="form-control" asp-for="Channel" />
                        <span class="form-text" asp-validation-for="Channel"></span>
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-4 text-sm-end align-self-center">
                    </div>
                    <div class="col-8">
                        <button type="submit" class="btn btn-outline-primary">Add Child to @Model.Channel</button>
                    </div>
                </div>
            </form>
        </div>*@

<div class="col-md-6 col-sm-12 mx-auto mb-5">
    <form method="post">
        <div class="mb-3 row">
            <div class="col-sm-4 text-sm-end align-self-center">
                <label class="form-label" asp-for="Channel.Title"></label>
            </div>
            <div class="col">
                <input class="form-control" asp-for="Channel.Title" />
                <span class="form-text" asp-validation-for="Channel.Title"></span>
            </div>
        </div>
        <div class="mb-3 row">
            <div class="col-sm-4 text-sm-end align-self-center">
                <label class="form-label" asp-for="Channel.Slug"></label>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-4 text-sm-end align-self-center">
            </div>
            <div class="col-8">
                <button type="submit" class="btn btn-outline-primary">Do the thing!</button>
            </div>
        </div>
    </form>
</div>

<div>
    <a asp-page="/Index">Back to List</a>
</div>